don't support device  ~-~ 不支持该设备
scene.zip error, don't contain rds.scene ~-~ 场景文件压缩包格式错误，不包含rds.scene文件
scene cannot be uploaded during task execution ~-~ 运单执行过程中无法上传场景，等待运单执行完成
scene data wrong ~-~ 场景文件数据错误
parse json error ~-~ 请求数据不为json，请求错误
core is not activated! ~-~ RDSCore未激活！
doesn't contain id or id is not a string ~-~ 运单id需要为string
block in blocks array must be an object ~-~ 运单中的blocks字段需要为object array
need to contain blockId or location in block ~-~ block中需要包含blockId和location
blockId and location need to be string ~-~ blockId和location需要为string
order details is empty ~-~ 运单不存在
